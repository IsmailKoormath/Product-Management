import React from "react";
import "./AddButton.css";

const DiscardButton = ({onClick}) => {
  return <button className="AddButton DiscardButton" onClick={onClick}>discard</button>;
};

export default DiscardButton;
